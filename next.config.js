/** @type {import('next').NextConfig} */
const nextConfig = {
   //esta parte de aca permite usar la etiqueta Image
   images: {
      domains: ["i.pinimg.com", "res.cloudinary.com"],
   },
};

module.exports = nextConfig;
